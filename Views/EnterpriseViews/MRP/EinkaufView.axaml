<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Percuro.ViewModels.EnterpriseViewModels.MRP"
             x:Class="Percuro.Views.EnterpriseViews.MRP.EinkaufView"
             x:DataType="vm:EinkaufViewModel">

    <Grid>
        <!-- Überschrift -->
        <TextBlock Text="Einkauf" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0" FontSize="24"/>

        <!-- To MRP Button -->
        <Button Content="To MRP" Command="{Binding ToMRPView}"
                Width="150" Height="50" Margin="10"
                HorizontalAlignment="Left" VerticalAlignment="Top"
                HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
        <TextBlock Text="Hallo Freunde" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</UserControl>