<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Percuro.ViewModels.EnterpriseViewModels.Finance"
             x:Class="Percuro.Views.EnterpriseViews.Finance.BilanzView"
             x:DataType="vm:BilanzViewModel">
    <Grid>
        <!-- To Finance Button -->
        <Button Content="To Finance" Command="{Binding ToFinanceView}" 
                Width="150" Height="50" Margin="10" 
                HorizontalAlignment="Left" VerticalAlignment="Top"
                HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>

        <!-- Überschrift -->
        <TextBlock Text="Bilanz View" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center" 
                   FontSize="20" />
    </Grid>
</UserControl>