<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Percuro.ViewModels.EnterpriseViewModels.CRM"
             x:Class="Percuro.Views.EnterpriseViews.CRM.SalesManagementView"
             x:DataType="vm:SalesManagementViewModel">
    <Grid>
        <!-- To CRM Button -->
        <Button Content="To CRM" Command="{Binding ToCRMMainView}" 
                Width="150" Height="50" Margin="10" 
                HorizontalAlignment="Left" VerticalAlignment="Top" 
                HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
        <TextBlock Text="Sales Management View" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24"/>

    </Grid>
</UserControl>